docs/
.browser-debug/
.DS_Store
node_modules/
/package.json
/package-lock.json
cli/bundle/
cli/node_modules/

# power/skills/ is GENERATED by core/build-bundle.mjs but is intentionally
# committed: Kiro installs the Power straight from the GitHub repo, so the
# skill files must be present there. Run `npm run build` in core/ after
# editing SKILL.md or references/, and commit the result.
